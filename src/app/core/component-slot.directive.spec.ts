import { ComponentSlotDirective } from './component-slot.directive';

describe('ComponentSlotDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentSlotDirective(null);
    expect(directive).toBeTruthy();
  });
});
